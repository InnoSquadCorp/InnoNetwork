<!-- release-status: draft -->
# InnoNetwork 6.1.0 Release Notes

Status: Draft (unreleased)

Release date: TBD

This candidate is layered after the local 6.0 release cut. It must not be
tagged or published before InnoNetwork 6.0.0 and its required consumer gates
have completed.

## Added

- Bounded transport admission with independent request/stream capacity, queue
  deadlines, origin partitioning, and structured policy decisions.
- Advanced monotonic token-bucket and sliding-window rate limiting, bounded
  scope state, `Retry-After`, and opt-in IETF draft-11 response feedback.
  Invalid numeric policies fail closed, dormant replenished scopes are
  reclaimed, quota is rechecked at physical dispatch after admission, and a
  valid `Retry-After` takes precedence when both feedback headers are present.
- Custom response caches can persist and restore
  `CachedResponse.rfc9111InitialAge`, preserving RFC 9111 age calculations
  across process restarts.
- Phase-aware streaming timeouts plus control-aware SSE cursor/retry metadata
  and bounded reconnect-on-EOF behavior.
- Vendor-neutral logical request and child physical-attempt spans with bounded
  export. Cache hits and coalesced followers do not fabricate attempt spans.
- Server-confirmed resumable upload chunks with atomic, credential-free
  checkpoints and one private immutable snapshot shared by incremental identity
  hashing and chunk reads.
- Caller-owned streaming cursors with validated custom header names, 4 KiB
  cursor bounds, explicit reset semantics, transient-only resume, and no
  automatic redirects. Cursor contents remain application-owned and are not
  persisted or interpreted as an exactly-once acknowledgement.
- Response-scoped `makeDecoder()` factories and an opt-in aggregate SSE event
  byte limit. SSE empty data, multiline whitespace, inherited IDs, metadata-only
  blocks, BOM position, and CR/LF/CRLF framing are corrected. Stateful consumers
  should follow the migration in the DocC `StreamingGuide`.
- `NetworkOperationDeadline` adds an opt-in, operation-wide monotonic latency
  budget for buffered `OperationNetworkClient` requests.
- `NetworkOperationDeadlineStage` and `NetworkFailure.deadlineStage` identify
  the coarse stage that exhausted the budget without retaining URLs, headers,
  bodies, or arbitrary underlying error text.
- Deadline recovery continues to require a replay-safe HTTP method or an
  explicit stable idempotency key. Coalesced callers retain independent
  budgets even when they share one underlying transport request.
- `UploadManager.pause(_:)` and `resume(_:)` add idempotent interactive
  controls, and `UploadState.paused` survives background restoration as
  durable user intent.
- `UploadManager.retry(_:with:fromFile:)` reuses a failed logical task only
  when the caller supplies refreshed inputs with the exact original
  application-owned `Idempotency-Key`. Destination and method changes,
  cancelled/completed tasks, missing source files, and changed keys fail
  before a replacement system task starts.
- `ResponseCachePolicy.staleIfError(wrapping:)` adds origin-authorized stale
  recovery for `500`, `502`, `503`, `504`, timeout, and reachability failures.
  The cached response must carry one valid `stale-if-error=N` directive and
  remain inside that window; `no-store`, `must-revalidate`, malformed or
  duplicate directives, cancellation, and non-transient failures fail closed.
- `ResponseCachePolicy.requestOnlyIfCached(wrapping:)` honors request
  `Cache-Control: only-if-cached` only when explicitly wrapped. A reusable
  cache hit returns without transport, stale-while-revalidate suppresses its
  background leg, and a miss or forced revalidation produces a typed local
  configuration failure.
- RFC-aware cache recovery now treats `no-cache` as mandatory validation,
  rechecks `stale-if-error` at final recovery time, clamps oversized server
  delta-seconds, and invalidates stored entries when a `304` changes `Vary`.
- Cache mutations are ordered per target URI and shared across clients made
  from the same configuration, so an older GET cannot write after an unsafe
  success or storage prohibition. `Vary: *` evicts a previous entry and its
  stale recovery path, mismatched `304` ETags fail closed under strong/weak
  validator rules, and changed-`Vary` callers receive
  the merged validation headers even though the old cache selection is removed.
- Cache response age starts at physical transport dispatch rather than before
  custom policy, admission, or local quota waits, including multi-dispatch
  policies that return an earlier response. Draft-11 RateLimit feedback parses
  the first RFC 9651 Structured Field policy and ignores the whole field when
  a member is malformed or omits required quota metadata.
- Admission queue waits use an absolute monotonic deadline at the grant
  boundary, including the derived long-lived stream policy.
- Post-audit lifecycle hardening keeps active upload retries controllable,
  emits one terminal event per logical request, retains live circuit-breaker
  probes and committed quota reservations, and records the initial operation
  deadline stage before racing the timer.
- Final contract hardening isolates half-open probes from older coalesced
  transports, preserves cancellation across decoding interceptors, makes
  streaming total deadlines independent of noncooperative callbacks, records
  authentication replays as distinct physical spans, and enforces exact-case
  upload retry methods.
- Completion hardening makes operation deadlines absolute from `start`, reports
  admission and transport stages at their execution boundaries, stops cancelled
  streaming interceptor/authentication/signer chains, resumes only recoverable
  first-event and idle watchdog expirations, and separates buffered transport
  span completion from decoding while leaving streaming spans open through the
  stream lifetime.
- Final boundary hardening stops buffered callback chains after cancellation,
  enforces streaming deadlines by absolute completion time, revalidates
  watchdog activity before timeout, prevents invalid SSE cursors from entering
  cursorless EOF/error reconnect paths, and closes physical streaming spans
  before reconnect delay while preserving header status.
- Cache correctness hardening releases in-memory LRU nodes with the cache,
  computes RFC 9111 current age from `Age`, `Date`, and transport response
  delay, and fails closed for malformed or overflowing `Age` values. A `304`
  revalidation and a persistent-cache reopen preserve corrected initial age;
  legacy persistent entries remain decodable and recompute a conservative age.
- A decoded streaming frame must now pass an atomic watchdog admission check
  before its cursor/retry metadata is committed or its event is yielded. A
  frame decoded at or after expiry is discarded and cannot seed a reconnect.
- Cancelling a `NetworkOperation` handle or the task awaiting `value()` now
  returns `.cancelled` promptly, including the no-deadline path, without
  waiting for cancellation-noncooperative application work to finish.

Existing operation starts without a deadline are source- and
behavior-compatible with 6.0. URLSession request/resource timeouts remain
separate transport controls, and separately returned streaming sequences are
not covered by the buffered operation deadline.

## Candidate gates

- [ ] All 6.0 publication and consumer gates complete first.
- [x] Public API, provisional-enum, DocC, and changelog contract gates pass.
- [x] Deterministic deadline tests cover cancellation, retry delay, stage
  reporting, timer cleanup, and coalesced callers with different budgets.
- [x] Managed-upload tests cover idempotent controls, restoration of durable
  paused intent, legacy active restoration, retry event partitioning, and
  stable idempotency-key enforcement.
- [x] Cache-control tests cover hit/miss identity, no-background
  only-if-cached behavior, persistent reopen, malformed directives,
  cancellation exclusion, acceptable-status overrides, retry exhaustion, and
  retry success taking precedence over stale recovery. Additional regressions
  cover LRU deallocation, `Age`/`Date`/response-delay accounting, exact
  freshness boundaries, `304` revalidation, and legacy/current persistent
  cache records.
- [x] The final root run passes all 1,788 registered tests; the four
  network-dependent live tests remain explicitly skipped unless `INNO_LIVE=1`.
  The final full-preflight sample reports runtime coverage of 88.04% by line,
  86.00% by function, and 80.34% by region; macro coverage is 90.22% by line,
  91.89% by function, and 88.73% by region. Exact per-run values remain in the
  generated coverage artifact.
- [x] The complete 13-gate set passes for implementation commit `c058f35`,
  including 11 consumer examples, two CycloneDX SBOM profiles, nine public
  DocC archives, and all nine public library targets on macOS, iOS, tvOS,
  watchOS, and visionOS. The final same-runner three-pair comparison passes all
  23 benchmarks with zero guard failures against the 20% regression threshold.
- [x] Admission, advanced quota, streaming timeout/control, span hierarchy,
  upload resource-bound, and resumable-upload interruption tests pass locally.
- [x] The final correctness pass covers immediate scope bounds, cross-origin
  admission progress, actual-dispatch quota rechecks, invalid numeric quotas,
  dormant-scope reclamation, total stream deadlines while quota-blocked,
  cache-hit span cardinality, pre-cancelled empty uploads, immutable source
  replacement, and post-finalize checkpoint cleanup failure. Follow-up
  adversarial coverage canonicalizes equivalent origins, retains quota scopes
  with suspended waiters, binds half-open probes to their owning attempts,
  applies total deadlines to monitor waits, reserves upload capacity before
  actor reentry, normalizes mutated span policies, and preserves terminal
  request events through saturated partition and observer queues.
- [x] The post-audit regression pass covers terminal-retention pressure during
  upload retry, pre-cancelled retry dispatch, decode-failure finalization,
  retry span cardinality, saturated terminal-failure delivery, live half-open
  probe retention, in-flight `Retry-After` feedback, and zero-budget operation
  deadline stage attribution.
- [x] The final contract regression pass covers fresh half-open transport
  ownership, post-decode cancellation from all three public cancellation
  paths, noncooperative streaming-deadline return and late resource cleanup,
  authentication replay span cardinality, and exact-case upload retry methods.
  Full-suite concurrency tests use event-driven start/cancellation signals and
  assert bounded-stream capacity without depending on scheduler-specific drop
  timing.
- [x] The completion regression pass covers exact zero and late operation
  deadlines, policy-admission versus transport stage attribution, streaming
  interceptor/signer/authentication cancellation, recoverable watchdog resume,
  transport-source span timestamps, post-transport decode failure, HTTP status
  retention, and streaming-header span lifetime.
- [x] The final boundary regression pass covers cancellation between buffered
  callbacks, delayed streaming timers, timely and expired first-event/idle
  activity races, metadata-only EOF deadline enforcement, terminal
  first-response deadlines, invalid-cursor EOF reconnect suppression, and
  streaming span closure before reconnect delay without duplicate public
  response events. The coalesced-deadline test now waits for two callers to
  join the production `RequestCoalescer` before advancing its virtual clock,
  so full-suite execution does not depend on caller task scheduling order.
- [x] The cache-age and operation-lifecycle regression pass covers cache-node
  deallocation, upstream age preservation across `304` and disk persistence,
  decoded-frame rejection before output/cursor mutation, prompt handle
  cancellation, and structured cancellation of the task awaiting `value()`.
